/* Jason Project

   See Prometheus specification in doc folder

*/

MAS domestic_robot {
    infrastructure: Centralised
    environment: HouseEnvOwn(gui) // use "nogui" as parameter to not show the GUI
    agents:
	car;
	motor;
	bicycle;
	restaurant;
	supermarket;
}

